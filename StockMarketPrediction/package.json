{
  "scripts": {
    "test": "mocha --no-timeouts --parallel --reporter mochawesome --require mochawesome/register",
    "pred": "mocha --no-timeouts --parallel --reporter mochawesome --require mochawesome/register test/Prediction"
  },
  "name": "wiserweb_test",
  "version": "1.0.0",
  "description": "WiserWeb_Test",
  "main": "index.js",
  "author": "Berk Gümüş",
  "license": "MIT",
  "dependencies": {
    "chromedriver": "^120.0.1",
    "mocha": "^10.2.0",
    "mochawesome": "^7.1.3",
    "selenium-webdriver": "^4.16.0"
  }
}
